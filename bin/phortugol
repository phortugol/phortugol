#!/usr/bin/env php
<?php

declare(strict_types = 1);

use Phortugol\Console\Kernel;
use Phortugol\Console\Presenters\TermwindPresenter;

$vendorPath = dirname(__DIR__) . '/vendor/autoload.php';
$localPath = dirname(__DIR__, 4) . '/vendor/autoload.php';

$autoload = match(true) {
    file_exists($vendorPath) => $vendorPath,
    file_exists($localPath) => $localPath,
    default => null,
};

if ($autoload === null) {
    fwrite(STDERR, "Autoloader not found. Run: composer install\n");
    exit(1);
}

require_once $autoload;

exit(new Kernel(new TermwindPresenter())->handle($_SERVER['argv']));
